# 股票第一批 · 无人值守推进进度日志

> Hans 离开期间自主连续推进。全程:CI 绿待核收 · **绝不合并、绝不部署** · 与 PR-8 隔离 · 守虚拟红线+分析非建议。
> 每完成一步落档于此,Hans 回来给完整核收材料。

---

## 任务1 · 港股 K线源深度复核(硬前置)— ✅ 完成(只读·结论已定)

### 核心结论:港股技术指标**能做**(深度充足),但覆盖面 = 策展池 18 只(非全市场)

| 维度 | 事实 | 证据 |
|---|---|---|
| **K线主源** | 新浪 `stock_hk_daily(adjust="qfq")`·返回**完整历史**(多年日线·前复权) | `hk_source.py:208-246` `_fetch_sina_daily`(`df.sort_values("date").tail(limit)`) |
| **备用源** | yfinance(`period="max"`·日线降级/周线直接用) | `hk_source.py:256-309` |
| **采集深度** | 每只策展股存 **300 根日线**(`_HK_POOL_LIMIT=300`)→ 技术指标(MA20/MACD26/RSI14/布林20 需 20-60 根)**充足** | `incremental.py:32,123` `update_hk_pool` |
| **K线覆盖标的** | **策展池 HK_POOL = 18 只**(腾讯/阿里…·有 300 根 K线深度) | `hk_pool.py:15-` `update_hk_pool` 遍历 HK_POOL |
| **spot 快照覆盖** | 全市场 **~900 只**(新浪前 15 页)·字段 price/change_pct/volume/amount·**无 K线** | `hk_source.py:111-181` |
| **宽度(breadth)** | 全市场聚合(up/down/flat + 总额·**港股无涨跌停**) | `clickhouse_hk_market.py:28,138` |
| **周期** | 仅 **1d/1w**(无分钟线) | `hk_source.py:66` `_HK_SUPPORTED_PERIODS` |
| **指数** | **无港股指数采集**(恒指告警做不了·总纲已定不做) | market_index_snapshot 无 hk |

### ★"~18 根"误读澄清(解决现状盘点 vs 深挖冲突)
现状盘点说"港股 K线深度仅 ~18 根",**是误读**,真相是两个无关的 18:
1. **策展池 18 只标的**(HK_POOL·有 K线覆盖的标的数)—— 这是"18"
2. **probe 的 yfinance 用 `period="1mo"` 轻量探测**(`hk_kline_probe.py:68`)→ yf_rows ≈ 18-22(只验可达·**故意不拉 max**)—— 另一个"18"
→ **都不是真实 K线深度**。真实:主源新浪完整历史 + 采集存 300 根/只。**深挖判断正确(新浪源深度够)。**

### ★对任务2/3 港股范围的裁定
| 指标类 | 港股可否 | 覆盖面 |
|---|---|---|
| price / 涨跌幅 / 成交量 | ✅ | 全市场 ~900 只(spot 快照) |
| MA / MACD / RSI / 布林%B | ✅(日线·300根够) | **仅策展池 18 只**(全市场无 K线) |
| 宽度(hk_breadth_up_ratio) | ✅ | 全市场聚合 |
| 恒指 index | ❌ | 无采集(总纲已定不做) |
| 分钟线类 | ❌ | 港股只 1d/1w |

→ **港股告警 8 指标子集成立**(price/涨跌幅/量/MA/MACD/RSI/布林/宽度),但**技术指标 4 项覆盖面只有 18 只策展股**,价格/涨跌幅/量/宽度可覆盖全市场 ~900。**告警/筛选 UI 要诚实标注"港股技术指标仅覆盖主要成分股(策展池)"。**

### ⚠️ 需 Hans 真机/生产确认(代码逻辑支持·生产数据待验)
- 生产新浪 `stock_hk_daily` 实际可达率(代码注释称"实测 5/5 sina_ok",但需现场复核当前是否仍稳)。
- 生产 ClickHouse `kline` 表 market=hk 实际行数(确认 18 只策展股是否真存了 ~300 根)。
- 这两项是"生产数据确认",我只读代码无法验,标出来等 Hans。

**任务1 不碰任何代码,纯只读核查。**

---

## 任务2 · 功能3 港股告警 — ✅ 代码完成 + 自验全绿(CI 跑中·待确认绿)

**分支** `feat/hk-alerts` · **PR** [#103](https://github.com/Hans010101/midas/pull/103)(★待核收·勿合并)· 从 main 切·与 PR-8 隔离

### 改动(对称扩展之美:后端只动 registry.py 一个文件)
| 文件 | 改动 |
|---|---|
| `registry.py` | `_ALL_HK=(cn,us,crypto,hk)`;price/涨跌幅/量 + MA5/20/60/MACD/RSI/布林%B 扩 hk;新增 `_f_hk_breadth_up_ratio` + `hk_breadth_up_ratio` 指标;★缠论/板块/指数不扩 |
| `test_registry_hk.py` | 新增 6 测(8 指标含 hk · 宽度 fetcher 三态 · 缠论/板块/指数回归保护) |
| `alert-rules-section.tsx` | `MARKET_LABEL` 加 `hk='港股'`(下拉数据驱动·一行) |

### ★零改(market-agnostic 的优雅)
`alert_scan` worker(遍历所有 enabled 规则)· engine(`market in markets` 校验)· 路由 · `alert_rule.market`(本就自由 str)· 引擎 —— **全零改**。

### 自验(本地真实退出码)
ruff **0** · mypy **Success no issues** · pytest **23 passed**(6 新 + 17 回归)· 前端 pnpm lint **0**。

### ★红线落地
告警 = "你设的条件已达成"客观事实(如 RSI 触及 75),**非买入/卖出信号** · 全程只读不下单。

### ⚠️ 给 Hans 核收的诚实约束
- 港股 per-symbol 告警(含价格类)只覆盖有 K线的**策展池 18 只**(fetcher 统一走 K线);全市场 ~900 只仅市场级 `hk_breadth` 可覆盖。
- 需 Hans 真机确认生产 hk kline 表实际行数 + 设一条港股告警触发 → 收 TG 推送。

**任务2 CI:测试闸 + 前端闸 both completed success ✓**

---

## 任务3 · 功能1 选股筛选器 1a MVP — ✅ 代码完成 + 自验全绿(CI 跑中·待确认绿)

**分支** `feat/stock-screener-1a` · **PR** [#104](https://github.com/Hans010101/midas/pull/104)(★待核收·勿合并)· 从 main 切·与 PR-8/hk-alerts 隔离

### 改动(后端 6 + 前端 3)
| 文件 | 改动 |
|---|---|
| `clickhouse_client.py` | `select_klines_batch`(方案C · LIMIT N BY symbol · 照 select_kline + IN list 范式) |
| `schemas/screener.py`(新) | ScreenerFilters/Hit/Response |
| `services/screener.py`(新) | 预筛A → 候选限界(top 800)→ 批量算 RSI/均线 → 分页 |
| `api/v1/screener.py`(新) | POST /screener/{market}(Literal 校验) |
| `api/v1/__init__.py` | 注册 screener_router |
| `tests/services/test_screener.py`(新) | 9 测 |
| `lib/api/screener.ts` / `hooks/use-screener.ts` / `app/screener/page.tsx`(新) | 前端筛选页 |

### 性能(总纲方案 A + C·★不上 B)
方案A 轻量预筛(spot 快照价格/涨跌幅 · 秒级)→ 候选限界成交额 top 800(有界)→ 方案C select_klines_batch 批量取 K线复用 indicators 现算 RSI/均线 → 匹配 → 分页。

### 自验(本地真实退出码)
ruff **0** · mypy **0** · pytest **9 passed** · 前端 lint **0** · build **0**(/screener 进 build 4.17kB)。

### ★红线落地
结果"符合条件的标的"客观列表 + 后端 disclaimer 免责 · 非荐股 · 纯只读不下单 · 复用市场无感指标引擎 · 不碰 crypto perp/智能/托管 · 不碰架构。

### ⚠️ 给 Hans 核收注意
- `select_klines_batch` SQL(LIMIT N BY symbol)靠范式 + mock 测,真实执行需真机/生产验(本地无 CH)。
- 技术指标候选上限 candidate_capped(成交活跃前 800)· 港股仅策展池 18 只有 K线 · 前端已诚实标注。
- 筛选器未做 Pro 门控(只读行情)· 如需门控请告知。

---

## 任务4 · 功能1 筛选器 1b 穿越类 — ✅ 代码完成 + 自验全绿(同 PR #104 · CI 跑中)

**同分支 feat/stock-screener-1a 续做**(独立 commit · PR [#104](https://github.com/Hans010101/midas/pull/104) 含 1a+1b)

### 改动(复用算法非从零写)
- `strategy_signals.py` 加公开 helper `latest_macd_golden_cross`/`latest_kdj_golden_cross`(复用私有 `_macd_series`/`_kdj_series` + `_crosses_up` · ★纯增量不改现有 scan_*)
- 布林带宽收窄 + 量比:直接复用 `indicators.compute_boll`/`compute_volume_ratio`
- screener filters/hit 加 4 字段 · `_tech_pass` 加 4 判定 · 前端加 4 条件 UI + 结果展示
- ★缠论 1c 不做(放 Hans 回来单独阶段)

### 自验
ruff 0 / mypy 0 / pytest **30 passed**(含金叉 helper 平盘+跳涨构造金叉验证)/ 前端 lint 0 / build 0。

---

## 📋 无人值守产出总表(Hans 回来核收 · 全部 CI 绿待核收 · 绝未合并)

| 任务 | 分支 / PR | 状态 | 核收要点 |
|---|---|---|---|
| 任务1 港股K线源复核 | (只读·无分支) | ✅ 结论已定 | 主源新浪完整历史 + 采集300根 + 策展18只;"~18"是误读 |
| 任务2 港股告警 | `feat/hk-alerts` · [#103](https://github.com/Hans010101/midas/pull/103) | ✅ CI 双绿 | registry 对称扩展(只改1文件)· per-symbol 覆盖策展18 |
| 任务3 筛选器1a | `feat/stock-screener-1a` · [#104](https://github.com/Hans010101/midas/pull/104) | ✅ 代码绿·CI跑中 | 预筛A+批量C+候选top800·现成条件 |
| 任务4 筛选器1b | 同 #104(续 commit) | ✅ 代码绿·CI跑中 | 穿越类·复用算法·缠论1c未做 |

**★ 全程红线守住**:全虚拟/只读 · 分析非建议(客观列表+免责·非荐股·告警是条件已达成事实)· 不碰 crypto perp/智能/托管 · 不碰架构 · 与 PR-8 隔离。
**★ 绝未合并、绝未部署**:所有刀停在分支推送 + CI 绿 · 等 Hans 回来批量核收放行。
**待 Hans 真机验**:① 港股告警触发→TG 推送 ② 筛选器 cn/us/hk 真机筛 + `select_klines_batch` SQL 生产执行 ③ 决定筛选器是否 Pro 门控 / 加导航入口 / 1c 缠论是否做。

---

## 🏁 收官 · Hans 核收 + 上线部署(2026-07-01)

**核收**:Hans 逐项核收 + 我 8-agent 对抗审计(4 lens + 2 红线各 2 票 refuter)`overallClean=True` 一致 → 两 PR 放行。3 决策:① 筛选器先公开不门控 ② 加导航入口(股票市场页"选股"tab·加密天然排除)③ 1c 缠论暂不做。

**导航入口收尾刀**:`MarketHomePage`(cn/us/hk 股票市场专用共享组件)加"选股筛选器 →"入口 → `/screener`。★加密天然排除的**类型铁证**:`MarketKind = 'cn'|'us'|'hk'`(build 曾因 `market !== 'crypto'` 报 no-overlap,证明加密不是合法值)。并入 PR #104。

**合并 + 部署**(REST merge 端点绕 GraphQL 限流 · 分别部署 · 每次三件套):
| PR | 合并 | 部署 run | 结论 | 用时 | 三件套 |
|---|---|---|---|---|---|
| #103 港股告警 | `7273590` | 28484285301 | ✅ success | 19m26s | 全过 |
| #104 筛选器+导航 | `8270a34` | 28485216763 | ✅ success | 19m03s | 全过 |

**三件套(两次均)**:① 三镜像严格串行 build(naming-done 时间戳递增·零交错·根治 #91 并行 OOM 宕机)② api/worker/web Recreated+Started(api Healthy)·pg/ch/redis 未动 ③ 真 OOM=0(★update.sh 自带"防并行 OOM/全程无 OOM"文案会误命中 grep·排除后才是真数)④ curl api/health·web·/screener 全 200 ⑤ worker 重建健康。

**导航红线生产 HTML 铁证**:`/cn-market`·`/us-market`·`/hk-market` 含"选股筛选器"=1;`/crypto-market`=0。

**Hans 真机功能验全过** → 股票第一批(功能2 条件单 + 功能3 港股告警 + 功能1 筛选器 1a/1b + 导航)**整个交付完成 · 收官**。1c 缠论 Hans 定暂不做、留未来单独阶段。
