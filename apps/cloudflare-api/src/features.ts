/**
 * Commercial membership is paused for midas-trading.
 *
 * The legacy implementation remains in the repository so it can be restored
 * deliberately, but registered users currently receive full feature access
 * and no trial, invitation, redemption or academy membership rewards run.
 */
export const COMMERCIAL_MEMBERSHIP_ENABLED = false
